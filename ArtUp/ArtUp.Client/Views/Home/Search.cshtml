@model ArtUp.Client.Models.MainSearchViewModel

@using (Html.BeginForm("SearchResults", "Home", FormMethod.Post, new { @class = "search"}))
{
    <div class="col-sm-10">
        @Html.TextBoxFor(m => m.Query, new { @placeholder = "Найти проект" })
    </div>
    <div class="col-sm-2">
        <input type="submit" value="Найти">
    </div>
}